#include <bits/stdc++.h>
#include<iterator>
using namespace std;

int main(){
    vector<int>arr(2,0);
    arr.push_back(2);
    arr.push_back(3);
    arr.push_back(4);
    arr.push_back(5);
    for(int i=0; i<arr.size(); i++){
        cout<<arr[i]<<" ";
    }
    cout<<endl;
    return 0;
}
